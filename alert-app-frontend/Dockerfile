# Use a lightweight web server as a base image
FROM nginx:alpine

# Copy index.html into the image at the specified location
COPY index.html /usr/share/nginx/html

# Expose port 80 to allow outside access to your app
EXPOSE 80
