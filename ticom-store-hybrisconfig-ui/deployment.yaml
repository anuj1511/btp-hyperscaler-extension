apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-app-frontend
  labels:
    app: crud-app-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crud-app-frontend
  template:
    metadata:
      labels:
        app: crud-app-frontend
    spec:
      containers:
        - name: crud-app-frontend
          image: anujjadhav0215/crud-app-frontend:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: crud-app-frontend
  labels:
    app: crud-app-frontend
spec:
  ports:
    - name: http
      port: 3000
  selector:
    app: crud-app-frontend
---
apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: crud-app-frontend
spec:
  gateway: kyma-gateway.kyma-system.svc.cluster.local
  host: crud-app-frontend 
  service:
    name: crud-app-frontend
    port: 3000
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - GET
        - POST
        - PUT
        - DELETE